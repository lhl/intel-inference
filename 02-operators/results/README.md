# 02-operators Results

This directory is for captured operator-benchmark artifacts such as:

- backend and dtype capability checks
- GEMM benchmark logs and JSON summaries
- SDPA / attention benchmark logs and JSON summaries
- later operator-level profiling or trace artifacts

Files created here are ignored by default, except for this index file.
