# 01-hardware Results

This directory is for captured low-level benchmark artifacts such as:

- raw memory-bandwidth outputs
- raw compute-benchmark outputs
- telemetry logs
- summarized CSV or JSON exports

Files created here are ignored by default, except for this index file.
