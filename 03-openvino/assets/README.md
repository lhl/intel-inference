# 03-openvino assets

This directory is for ignored local sample assets used by `03-openvino`, such as speech files for Whisper smoke tests.

The tracked scripts should be able to recreate these assets when possible.
