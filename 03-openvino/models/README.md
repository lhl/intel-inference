# 03-openvino models

This directory is for ignored exported OpenVINO model artifacts used by the `03-openvino` phase.

Tracked docs should reference model aliases and Hugging Face IDs, not commit the converted model directories themselves.
