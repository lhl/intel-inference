# 03-openvino Results

This directory is for captured OpenVINO runtime artifacts such as:

- per-env capability checks
- device enumeration and device-property summaries
- synthetic OpenVINO device benchmark logs and JSON summaries
- later export, compile-cache, and model-level runtime results

Files created here are ignored by default, except for this index file.
