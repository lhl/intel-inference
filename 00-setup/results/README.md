# 00-setup Results

This directory is for captured setup artifacts such as:

- system inventory dumps
- driver and toolchain verification outputs
- env bring-up logs
- smoke-test command outputs

Files created here are ignored by default, except for this index file.
