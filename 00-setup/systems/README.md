# 00-setup Systems

This directory is for sanitized tracked system profiles that are safe to commit.

These files should contain benchmark-relevant machine context such as:

- distro and kernel
- CPU model
- RAM size
- GPU and NPU inventory
- relevant driver and tool visibility

They should not contain user names, group memberships, home-directory paths, or other avoidable personal details.
